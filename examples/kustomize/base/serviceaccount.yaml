# SPDX-License-Identifier: LicenseRef-DSL-1.0
# Deferred Source License (DSL)
# Pacer, Copyright (c) 2026 YouSysAdmin

apiVersion: v1
kind: ServiceAccount
metadata:
  name: pacer
  namespace: pacer
  # Annotate via overlay with the IRSA role ARN, e.g.
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/pacer
  # The role's trust policy must match
  #   system:serviceaccount:<namespace>:pacer
  # See docs/installation/server.md (Kubernetes section) for the
  # trust-policy JSON.
